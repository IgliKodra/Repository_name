import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.FinalProject.com',
  appName: 'Final Project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
